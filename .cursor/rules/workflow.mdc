# Remote Control - Инструкция для AI агентов

## 🎯 Назначение проекта

**Remote Control** - это macOS приложение для удаленного управления данными через REST API. Приложение позволяет подключаться к удаленным серверам, просматривать схемы данных и выполнять CRUD операции с записями.

## 🏗️ Архитектура проекта

### Основные компоненты:
- **SwiftUI** - пользовательский интерфейс
- **Combine** - асинхронные операции и реактивное программирование
- **URLSession** - HTTP клиент для API запросов
- **UserDefaults** - локальное хранение настроек

### Структура проекта:
```
Sources/RemoteControl/
├── App/                    # Точка входа приложения
├── Core/                   # Бизнес-логика
│   ├── Models/            # Модели данных
│   ├── Services/          # Сервисы (API, хранение)
│   └── Storage/           # Локальное хранение
├── Features/              # Функциональные модули
│   ├── Companies/         # Управление компаниями
│   ├── DataManagement/    # CRUD операции с данными
│   └── Schema/            # Работа со схемами
└── Shared/               # Общие компоненты
    ├── Components/        # Переиспользуемые UI компоненты
    └── Utils/            # Утилиты
```

## 🔧 Порядок сборки

### 1. Подготовка окружения
```bash
# Убедитесь, что установлен Xcode и Swift
swift --version
# Должна быть версия 6.2 или выше
```

### 2. Сборка проекта
```bash
# Сборка универсального бинарника (Intel + Apple Silicon)
./create_app.sh

# Или ручная сборка для конкретной архитектуры:
swift build -c release --arch x86_64    # Intel
swift build -c release --arch arm64     # Apple Silicon
```

### 3. Процесс сборки (create_app.sh)
1. **Очистка** предыдущих сборок
2. **Сборка для Intel** (x86_64)
3. **Сборка для Apple Silicon** (arm64)
4. **Создание универсального бинарника** с помощью `lipo`
5. **Создание app bundle** с подписью кода
6. **Проверка архитектур** финального файла

## 🌐 API интеграция

### Базовые настройки:
- **Минимальная версия macOS**: 12.0
- **Авторизация**: Заголовок `X-Remote-Secret`
- **Формат данных**: JSON

### API эндпоинты:
```
GET    /api/remote/check-access    # Проверка соединения
GET    /api/remote/schema          # Получение схемы данных
GET    /api/remote/{table}         # Список записей (с пагинацией)
GET    /api/remote/{table}/{id}    # Одна запись
POST   /api/remote/{table}         # Создание записи
PUT    /api/remote/{table}/{id}    # Обновление записи
DELETE /api/remote/{table}/{id}    # Удаление записи
```

### Модели данных:
- **Company**: URL, имя, секретный ключ
- **Schema**: Таблицы с полями и типами
- **DataRecord**: Динамические записи с любыми полями
- **PaginationInfo**: Информация о пагинации

## 📱 Основной функционал

### 1. Управление компаниями
- Создание/редактирование/удаление компаний
- Хранение URL и секретных ключей
- Активация компаний для работы

### 2. Подключение к серверам
- Тестирование соединения
- Проверка авторизации (401/403/200)
- Получение схемы данных

### 3. Работа с данными
- Просмотр таблиц в виде сетки карточек
- CRUD операции через полноэкранные формы
- Динамические формы на основе схемы
- Пагинация данных

### 4. UI/UX особенности
- Адаптивный сайдбар с изменяемой шириной
- Навигация между таблицами и записями
- Индикаторы статуса соединения
- Поддержка темной темы

## 🛠️ Правила разработки

### 1. Код-стиль
- Используйте SwiftUI для всех UI компонентов
- Применяйте MVVM архитектуру с @ObservableObject
- Все сетевые запросы через APIClient
- Локализация на русском языке

### 2. Обработка ошибок
- Все API ошибки обрабатываются в APIClient
- Показывайте пользователю понятные сообщения
- Логируйте ошибки для отладки

### 3. Тестирование
- Добавляйте print() для отладки критических операций
- Тестируйте на обеих архитектурах (Intel/Apple Silicon)
- Проверяйте работу с реальными API

### 4. Безопасность
- Никогда не логируйте секретные ключи
- Используйте ad-hoc подпись кода для локальной разработки
- Валидация данных на стороне сервера

## 🔍 Отладка

### Логи для отслеживания:
```
🏢 RemoteControlApp: - изменения активной компании
🔍 DataService: - загрузка данных
📡 APIClient: - HTTP запросы
🔘 TableDataView: - навигация между формами
```

### Консоль для просмотра логов:
```bash
# Откройте Console.app для просмотра логов приложения
open /System/Applications/Utilities/Console.app
```

## 📦 Развертывание

### Создание дистрибутива:
1. Запустите `./create_app.sh`
2. Проверьте архитектуры: `lipo -info RemoteControl.app/Contents/MacOS/remoteControl`
3. Протестируйте на целевых системах
4. Архивируйте `RemoteControl.app`

### Системные требования:
- **macOS 12.0+**
- **Intel x86_64** или **Apple Silicon arm64**
- **Интернет соединение** для API запросов

## ⚠️ Важные замечания

1. **Универсальная сборка**: Всегда используйте `./create_app.sh` для создания приложения
2. **API совместимость**: Убедитесь, что сервер поддерживает требуемые эндпоинты
3. **Авторизация**: Используйте заголовок `X-Remote-Secret` для всех запросов
4. **Схема данных**: Приложение динамически адаптируется под схему сервера
5. **Навигация**: Используйте полноэкранные формы вместо модальных окон

## 🚀 Быстрый старт

1. **Клонируйте проект**
2. **Запустите сборку**: `./create_app.sh`
3. **Откройте приложение**: `open RemoteControl.app`
4. **Добавьте компанию** с URL и секретным ключом
5. **Протестируйте соединение** и получите схему данных
6. **Начните работу** с таблицами и записями

---
*Последнее обновление: $(date)*
*Версия проекта: 1.0*